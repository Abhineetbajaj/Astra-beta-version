# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — never commit real values. See .env.example / supabase/functions/.env.example.
.env
.env.*
!.env.example
!.env.*.example

# Local agent/CLI tooling, installed per-machine (Higgsfield skills and friends) — not product
# code. Delete these three lines if the team later decides to version the skill set deliberately.
.agents
.claude
skills-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
